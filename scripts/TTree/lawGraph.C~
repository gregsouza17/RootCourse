double Uavarage(int N);


void lawGraph(){
  
}

double Uavarage(int N){
  static TRandom3 RandGen(std::time(0));
  
  double avg(0);
  for(int i=0; i<N; i++){
    avg+=RandGen.Uniform();
  }

  return avg/N;
  
}
